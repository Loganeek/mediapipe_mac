name: Build

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  # workflow_dispatch:
  #  inputs:
  #     version: 
  #       description: '版本号'
  #       required: false

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        # You can add more, for any target you'd like!
        include:
          - name: macos-latest
            os: macos-latest
            

    steps:
    - uses: actions/checkout@v4

    # - name: build
    #   shell: bash
    #   run: |
    #     uname -m

    - name: ready enviroment
      shell: bash
      run: |
        # brew install pyenv
        brew uninstall --ignore-dependencies python3
        brew reinstall python@3.12

    # - name: install python 3.12
    #   shell: bash
    #   run: |
    #     python --version
    #     pyenv install 3.12
    #     pyenv global 3.12
        
    #     export PATH="$HOME/.pyenv/bin:$PATH"
    #     eval "$(pyenv init - bash)"
    #     python --version
    #     pip install setuptools
        
    #- name: setup_opencv
    #  shell: bash
    #  run: |
    #    chmod +x ./setup_opencv.sh
    #    ./setup_opencv.sh

    - name: build
      shell: bash
      run: |
        uname -m
        # export PATH="$HOME/.pyenv/bin:$PATH"
        # eval "$(pyenv init - bash)"
        python --version
        
        # export HERMETIC_PYTHON_VERSION=3.12
        bazel build -c opt --config ios_arm64 --define OPENCV=source --strip always --define MEDIAPIPE_DISABLE_GPU=1 //mediapipe/tasks/c/vision/hand_landmarker:libhand_landmarker.dylib

    # - name: build
    #   shell: bash
    #   run: |
    #     uname -m
    # #    bazel build -c opt --define MEDIAPIPE_DISABLE_GPU=1 //mediapipe/tasks/c/vision/object_detector:object_detection_tflite_speed_test
    # # chmod +x ./build_desktop_examples.sh
    # # ./build_desktop_examples.sh

    # - name: check binary
    #   shell: bash
    #   run: |
    #     file bazel-bin/mediapipe/tasks/c/vision/object_detector/object_detection_tflite_speed_test

    # - name: Upload archive
    #   uses: actions/upload-artifact@v4.5.0
    #   with:
    #     name: object_detection_tflite_speed_test
    #     path: bazel-bin/mediapipe/tasks/c/vision/object_detector/object_detection_tflite_speed_test
